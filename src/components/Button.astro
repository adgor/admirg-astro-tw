---
import Icon from "astro-icon";

export interface Props {
  btnColorStyle?: string;
  iconSpan?: string;
  iconName?: string;
  target?: boolean;
  link: string;
}
const {
  btnColorStyle = "bg-brand-purple text-brand-culturedDark hover:text-white",
  iconSpan,
  iconName = "ic:twotone-arrow-right-alt",
  target = false,
  link,
} = Astro.props;
---

<a
  target={target && "_blank"}
  href={link}
  class={`inline-flex items-center tracking-wider px-8 py-4 text-xs  duration-700 transition-colors uppercase rounded-lg group ${btnColorStyle}`}
>
  <slot />
  <span class={`flex items-center ml-2 ${iconSpan}`}>
    <Icon
      name={iconName}
      class="h-5 w-5 transform duration-500 group-hover:translate-x-1"
    />
  </span>
</a>
